server:
   port: 8888

logging:
    path: logs
    file: mylog.log


spring:
   application:
          name: userManager
   datasource:
       driver-class-name: com.mysql.cj.jdbc.Driver
       url: jdbc:mysql://localhost:3306/testng?useSSL=false
       username: cxk
       password:

mybatis:
    type-aliases-package: com.course.model
    mapper-locations:
       - mapper/*
